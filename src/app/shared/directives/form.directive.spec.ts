/*
import { FormDirective } from './forms.directive';

describe('FormDirective', () => {
  it('should create an instance', () => {
    const directive = new FormDirective();
    expect(directive).toBeTruthy();
  });
});
*/
