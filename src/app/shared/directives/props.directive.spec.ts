/*
import { PropsDirective } from './props.directive';

describe('PropsDirective', () => {
  it('should create an instance', () => {
    const directive = new PropsDirective();
    expect(directive).toBeTruthy();
  });
});
*/
