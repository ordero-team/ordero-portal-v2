import { NoNegativeDirective } from './no-negative.directive';

describe('NoNegativeDirective', () => {
  it('should create an instance', () => {
    const directive = new NoNegativeDirective();
    expect(directive).toBeTruthy();
  });
});
